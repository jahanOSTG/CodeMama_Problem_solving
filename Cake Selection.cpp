#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    if (a >= 200 && a <= 300 && b >= 50 && c >= 150) {
         cout << "Yes" << endl;
            }
    else {
            cout << "No" << endl;
        }
   

    return 0;
}
